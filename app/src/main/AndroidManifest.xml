<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- 네트워크 사용을 위한 퍼미션 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.camera.any" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Android 12 이상 권장: 알림 권한 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"
        tools:targetApi="33" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:usesCleartextTraffic="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.CSPF"
        tools:targetApi="31">

        <!-- Google Maps API Key -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyD7EMaiYLr35L2ssPQpiHkjdrwd0KthF0A"/>

        <!-- 메인 액티비티: 로그인 화면 -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 회원가입 액티비티 -->
        <activity
            android:name=".auth.SignupActivity"
            android:exported="true" />

        <!-- 메인 액티비티 -->
        <activity
            android:name=".MainActivity"
            android:exported="true" />

        <activity android:name=".ProfileActivity"
            android:exported="true" />

        <activity android:name=".insurance.InsuranceInfoActivity" />

        <!-- 챗봇 액티비티 -->
        <activity android:name=".chatbot.ChatActivity"
            android:exported="true"/>
        <activity
            android:name=".chatroom.ChatRoomActivity"
            android:exported="true" />
        <!-- 공지사항 세부 액티비티 -->
        <activity android:name=".notices.NoticeDetailActivity" />

        <!-- 문의사항 세부 액티비티 -->
        <activity android:name=".inquiries.InquiryDetailActivity" />

        <!-- 문의사항 액티비티 -->
        <activity android:name=".inquiries.InquiriesActivity" />

        <activity android:name=".insurance.ProductDetailActivity" />



        <!-- Google 로그인 액티비티 -->
        <activity android:name=".auth.GoogleSignInActivity"/>




        <activity
            android:name=".chatroom.ChatRoomListActivity" />

        <activity android:name=".CameraView" />
    </application>
</manifest>
